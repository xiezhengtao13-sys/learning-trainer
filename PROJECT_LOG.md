# 项目日志：三线学习训练器

日期：2026-06-29

## 项目目标

为日语、英语和《逻辑哲学论》做一个适合个人长期使用的学习程序。核心学习偏好是：

- 少做孤立背诵，多用课文、长句和上下文记忆。
- 练习要有互动感、即时反馈和可见进度。
- 题库和推荐形式要根据每日学习内容、掌握情况和偏好逐步调整。
- 主要使用场景是在 iPhone 上通勤学习，路线为南与野 → 赤羽 → 王子 → 东大前。

## 当前实现

项目是一个静态 Web App，主要文件：

- `index.html`：入口页面和 PWA 元信息。
- `app.js`：题库、状态管理、推荐算法、通勤模式、同步逻辑。
- `styles.css`：桌面和 iPhone 移动端样式。
- `manifest.webmanifest`：PWA 配置。
- `service-worker.js`：HTTPS 环境下缓存核心文件。
- `icon.svg`：主屏幕图标。
- `README.md`：安装、使用、部署和同步说明。

## 功能演进记录

### 1. 初始学习程序

完成了本地单页应用：

- 三个学习方向：日语、英语、《逻辑哲学论》。
- 支持选择题、填空题、组句题、自评题。
- 支持“忘了 / 模糊 / 会了”评分。
- 使用本地 `localStorage` 保存进度。
- 初步实现到期复习、弱项加练、混合挑战。

### 2. 改为课文和长句驱动

根据学习偏好，降低孤立词汇比重，增加：

- 日语“课文长句”题组。
- 英语“长句精读”题组。
- 哲学“段落精读”题组。
- 题卡上下文、要点、译文。
- 根据错题自动生成巩固题。

关键原则：先用上下文理解和记忆，再用题目检查能否主动回忆。

### 3. 每日学习记录和智能推荐

新增“今日记录”：

- 今天学了什么。
- 今天想练什么形式。
- 当前状态：轻量、正常、加强。
- 今天卡住的点。

推荐算法从复杂分数改为更可解释的篮子策略：

1. 到期复习。
2. 今日内容。
3. 弱项修补。
4. 偏好形式。
5. 未见课文/长句。

保存今日记录后，会自动生成：

- 今日复述题。
- 今日默写题。
- 难点解释题。

### 4. iPhone 移动端优化

针对 iPhone 使用做了界面调整：

- 科目、题组、模式改为横向滑动条。
- 练习卡优先出现在首屏。
- 提交和评分按钮固定在底部安全区上方。
- 输入框字号调整为 16px，避免 iOS 自动放大。
- 添加 `viewport-fit=cover` 和 iOS Web App 元信息。

### 5. 通勤模式

根据实际路线做分段推荐：

- 站台/换乘：3 题，单手选择题，随时可停。
- 南与野 → 赤羽：8 题，适合课文和长句。
- 赤羽 → 王子：5 题，短段快练，不安排复杂输入。
- 王子 → 东大前：10 题，适合长句、默写、复述和哲学段落。

通勤队列会跨科目选题，同时考虑今日记录、到期卡、弱项和当前路段适合的题型。

### 6. 手机和电脑数据交互

第一步实现了手动同步：

- 生成备份 JSON。
- 复制或下载备份。
- 在另一台设备导入并合并。

合并策略：

- 自定义题和每日记录按 ID 合并。
- 学习进度保留练习次数更多或更新时间更晚的版本。
- 历史记录按时间合并。

### 7. GitHub 云同步

为了满足电车上脱离局域网使用，加入 GitHub 方案：

- GitHub Pages 用于发布网页。
- 私有 GitHub Gist 用于保存学习进度 JSON。
- 手机和电脑打开同一个页面后，可以自动“拉取合并 → 上传”。

安全处理：

- GitHub token 只保存在当前浏览器的 `localStorage`。
- token 不会写入“生成备份”的 JSON。
- 建议 token 只授予 `gist` 权限。

### 8. 工程优化与可发布性

在功能基本稳定后，做了一轮以"能可靠发布到 iPhone"为目标的优化：

- Service Worker 由 cache-first 改为 network-first（应用外壳）+ cache-first（图标等静态资源）。这样发布新版本到 GitHub Pages 后，手机联网时会自动拿到最新代码，离线时仍回退缓存。
- Service Worker 不再拦截跨域 GET，避免把 GitHub Gist 的同步数据缓存成旧版本。
- 新增 `apple-touch-icon.png`（180×180）并在 `index.html` 中声明，修复 iOS"添加到主屏幕"图标不显示的问题。
- `shuffle` 由 `sort(random)` 改为 Fisher-Yates，消除出题顺序的系统性偏置。
- 增强健壮性：`loadState` 显式处理空/非法存储；今日目标为 0 时不再除零。
- `app.js` 增加环境判断，可在 Node 中被 `require`，并导出纯逻辑函数；新增 `test.js` 单元测试。

## 关键设计决策

### 为什么不是直接用 Git 命令同步？

iPhone Safari 不能直接运行 `git pull` / `git push`，所以纯 Git 命令不适合移动端网页。更合适的方案是：

- GitHub Pages 提供静态网页访问。
- GitHub Gist API 提供数据同步。

这相当于“用 GitHub 生态解决同步”，但不是让手机浏览器执行 Git 命令。

### 为什么不用局域网作为长期方案？

局域网方案只适合调试。通勤时手机无法访问家里或电脑局域网，因此长期方案必须是公网 HTTPS。GitHub Pages 满足这个条件。

### 为什么先不用 DeepSeek API？

DeepSeek 可以用于生成题目和推荐策略，但 API key 不适合直接放在前端页面中。更安全的后续方案是增加一个本地或云端代理服务，让前端不直接接触模型密钥。

## 当前验证记录

本次可复现的验证：

- `node --check app.js`、`node --check service-worker.js` 语法检查通过。
- `node test.js` 单元测试 8 项全部通过，覆盖 `normalize`、`shuffle`（含分布偏置检查）、关键词抽取、句子切分，以及按 ID / 进度 / 历史的合并逻辑（同步关键路径）。
- 浏览器静态服务（`python -m http.server`）下初始渲染正常，无 console 报错。
- 确认 GitHub token 不会进入导出 / Gist 的 JSON（`persistedState` 主动剥离 token）。

说明：智能推荐、通勤队列、今日生成题等依赖 DOM 和运行时 state 的流程，目前仍以人工验证为主。后续可把 `buildSmartQueue` / `buildCommuteSmartQueue` 中的纯逻辑拆出来补测试。

## iPhone 使用方案

长期推荐：

1. 将项目推送到 GitHub 仓库。
2. 开启 GitHub Pages。
3. 用 iPhone Safari 打开 Pages 地址。
4. 添加到主屏幕。
5. 在“数据同步 → GitHub 云同步”中配置 token。
6. 第一次点击“创建云端”。
7. 保持“联网时自动同步”开启。

通勤使用建议：

1. 上车前或站台先填“今日记录”。
2. 等车/换乘时选“站台/换乘”。
3. 南与野到赤羽做课文长句。
4. 赤羽到王子做短题。
5. 王子到东大前做默写、复述或哲学段落。

## 后续待办

- 增加 GitHub Pages 一键部署脚本或详细命令。
- 增加 DeepSeek/模型代理服务，用于根据今日记录生成更多高质量题目。
- 增加题库导入格式，例如 CSV/JSON 批量导入。
- 增加复习日历和通勤完成统计。
- 增加日语第 1-15 课更系统的原创课文题库。
- 增加英语土木工程和 AI 学术长句题库。
- 增加《逻辑哲学论》概念图和编号命题导读。

## 当前状态

项目已经可以作为本地/移动端学习工具使用，并完成了一轮工程优化（可发布、可更新、有单元测试）。代码层面已较为稳定，下一步回报最高的不是继续改引擎，而是：1) 真正发布到 GitHub Pages 并在 iPhone 配置 Gist 同步；2) 扩充题库内容（手写或接入 DeepSeek 代理生成），让推荐引擎有足够的题可调度。
