# 学习程序模式重构修改手册

日期：2026-07-06

状态：方案手册，不直接实施。  
目的：把后续修改从“零散加功能”改成“按主模式和子模式重构”，方便逐步实现、记录和复盘。

## 一项必须要求

在正式开始代码修改前，先新建一份独立的更新日志手册：

- 建议文件名：`UPDATE_LOG_MANUAL.md`
- 用途：记录每一次实际修改的日期、目标、改动文件、测试结果、遗留问题和下一步建议。
- 后续协作方式：每次修改后先更新 `UPDATE_LOG_MANUAL.md`，之后 Codex 可以只阅读这份更新日志手册，快速判断当前实现状态，并向你提出调整意见。

推荐日志条目模板：

```md
## YYYY-MM-DD 第 N 次修改

### 本次目标
- 

### 实际改动文件
- 

### 完成内容
- 

### 未完成/暂缓
- 

### 测试结果
- 

### 使用反馈
- 

### 下一步建议
- 
```

## 总体重构目标

现有项目已经有日语、英语、逻辑哲学论三条线，并且日语已有 N1 年度路线、AI 解析和阅读实验室雏形。下一轮重构的重点不是继续增加零散题，而是重新定义“主模式”和“子模式”。

新的目标结构：

- 日语：以“阅读 + 逐句解析”为主模式。
- 日语下属两个子模式：
  - 单词相关题目
  - 语法相关题目
- 英语：做类似阅读驱动改造，但只针对单词，不做语法子模式。
- 其他科目先不大改，避免影响范围过大。

## 新的信息架构

### 日语

日语应从“题组驱动”改为“阅读驱动”。

建议主入口：

- 日语阅读主模式
  - 阅读短文
  - 逐句解析
  - 点击/选取收藏生词
  - AI 实时追问
  - 根据掌握/忘记情况生成下一篇短文

日语子模式：

1. 单词模式
   - 来源：阅读短文中收藏的词、AI 标出的关键词、用户手动选中的词。
   - 题型：假名读音、中文释义、日译中、词义辨析、语境填空。
   - 调度依据：收藏次数、忘记次数、最近阅读中是否再次出现。

2. 语法模式
   - 来源：逐句解析里的句型、助词、接续、变形、N1/N2 表达。
   - 题型：选择正确接续、句子改写、语法点解释、组句、语境判断。
   - 调度依据：阅读卡评分、语法点忘记次数、AI 诊断出的弱项。

### 英语

英语也改为阅读驱动，但只做单词方向。

建议主入口：

- 英语阅读词汇模式
  - 阅读短句/短文
  - 点击/选取收藏生词
  - AI 解释单词在当前句中的意思
  - 自动生成词汇复习题

英语不做语法子模式。原因：

- 当前英语目标更偏 IELTS、工程和 AI 学术词汇。
- 最需要的是语境中识别和主动使用词汇。
- 语法如果以后要做，应作为单独阶段，不混进这次重构。

## 模块命名建议

### 日语模块

建议保留 `japanese` track，但重命名或重排 modules：

```js
modules: [
  { id: "jp-reading", name: "阅读解析" },
  { id: "jp-vocab", name: "阅读单词" },
  { id: "jp-grammar", name: "阅读语法" },
  { id: "jp-n1-plan", name: "N1路线" }
]
```

可以暂时保留旧模块 `jp-sentence`、`jp-listening`、`jp-output`，但不要作为主入口展示。它们可以作为内部标签或以后再合并。

### 英语模块

建议调整为：

```js
modules: [
  { id: "en-reading", name: "阅读词汇" },
  { id: "en-vocab", name: "生词复习" },
  { id: "en-civil", name: "土木词汇" },
  { id: "en-ai", name: "AI学术词汇" },
  { id: "en-ielts", name: "雅思表达" }
]
```

英语阅读模式只负责单词，不负责语法解析。

## 数据结构修改计划

### 阅读卡结构

建议统一日语和英语的阅读卡格式：

```js
{
  id: "reading-...",
  track: "japanese" | "english",
  module: "jp-reading" | "en-reading",
  type: "reading",
  prompt: "阅读短文：标题",
  level: "N4 → N3" | "IELTS 6.0",
  summary: "这篇短文练什么",
  sentences: [
    {
      text: "原句",
      reading: "日语假名或英语朗读辅助，可选",
      translation: "中文译文",
      grammar: ["仅日语使用"],
      words: [
        {
          text: "词",
          reading: "读音，可选",
          meaning: "中文意思",
          tags: ["n2", "academic", "civil"]
        }
      ]
    }
  ]
}
```

注意：当前代码中日语阅读卡字段使用 `jp/kana/zh`，下一轮重构时建议统一为 `text/reading/translation`，并写一个兼容函数读取旧字段，避免已有卡失效。

### 生词结构

建议统一 `jpVocab` 和英语生词，改成更通用的 `vocabBank`：

```js
{
  id: "vocab-...",
  track: "japanese" | "english",
  word: "単語",
  reading: "たんご",
  meaning: "单词",
  sourceSentence: "原句",
  sourceCardId: "reading-...",
  status: "new" | "forgot" | "known",
  correct: 0,
  lapses: 0,
  createdAt: "...",
  updatedAt: "..."
}
```

迁移方法：

- 读取旧 `jpVocab`。
- 读取旧英语 `customCards` 中 `module === "en-vocab"` 的词。
- 合并到 `vocabBank`。
- 暂时保留旧字段，避免同步数据丢失。

### 语法点结构（日语专用）

建议新增：

```js
grammarBank: [
  {
    id: "grammar-...",
    pattern: "からといって",
    meaning: "虽说因为……但不能……",
    connection: "普通形 + からといって",
    sourceSentence: "...",
    sourceCardId: "...",
    status: "new" | "forgot" | "known",
    lapses: 0,
    createdAt: "...",
    updatedAt: "..."
  }
]
```

语法点只服务日语，不要复用于英语。

## UI 修改计划

### 主导航

日语页面建议展示：

- 主按钮：开始阅读解析
- 子按钮：单词练习
- 子按钮：语法练习
- 辅助：N1路线、学习档案、阅读生词

英语页面建议展示：

- 主按钮：开始阅读词汇
- 子按钮：单词练习
- 辅助：英语生词本、工程/AI词汇

### 阅读卡 UI

阅读卡应包含：

1. 标题和难度
2. 完整短文
3. 逐句卡片
   - 原句
   - 读音/辅助朗读
   - 中文译文
   - 日语语法点
   - 可点击单词 chip
4. 收藏选中文本按钮
5. AI 追问输入框
6. 生成下一篇短文按钮
7. 忘了 / 模糊 / 会了评分

### 单词子模式 UI

日语和英语都可以共用：

- 看词写意思
- 看意思写词
- 听/读音识别
- 语境填空
- 近义词辨析

### 语法子模式 UI（日语）

只在日语出现：

- 选择正确接续
- 句子填空
- 句型改写
- 解释语法点
- 在阅读原句中定位语法作用

## 队列和推荐逻辑

### 日语推荐权重

建议智能推荐按以下顺序组队：

1. 今日阅读卡
2. 到期阅读卡
3. 忘记的单词题
4. 忘记的语法题
5. AI 生成的新短文
6. 未见阅读短文

阅读卡应该占主导，建议比例：

- 阅读解析：60%
- 单词题：25%
- 语法题：15%

如果用户当天状态是轻量：

- 阅读解析：50%
- 单词题：40%
- 语法题：10%

### 英语推荐权重

英语不做语法子模式，建议：

- 阅读词汇：65%
- 单词题：35%

## AI 修改计划

### 代理端点

建议保留并强化：

- `/chat`：阅读中实时追问
- `/reading-passage`：生成下一篇短文
- `/analyze`：单句解析
- `/diagnose`：阶段性诊断

### 日语短文生成提示词要求

AI 生成日语短文时必须读取：

- 当前 N1 周数和阶段
- 最近忘记的单词
- 最近忘记的语法点
- 最近阅读卡评分
- 今日记录
- 用户标记“掌握”的内容，避免重复太多

输出必须是结构化 JSON，不要只返回自然语言。

### 英语短文生成提示词要求

AI 生成英语短文时必须读取：

- 当前英语目标：IELTS / 工程 / AI 学术
- 收藏但未掌握的英语词
- 最近错的词汇题
- 用户已有专业方向：土木工程、AI

输出也应是结构化 JSON，但不需要 grammar 字段。

## 实施顺序

### 第一步：重命名和整理模块

- 调整日语 modules。
- 调整英语 modules。
- 不删除旧卡，只改变主入口和推荐权重。
- 更新 UI 文案，明确“阅读解析”为日语主模式。

### 第二步：统一阅读卡数据结构

- 定义统一的 `type: "reading"`。
- 写兼容函数，支持旧字段 `jp/kana/zh`。
- 让日语和英语阅读卡共用渲染框架。

### 第三步：统一生词系统

- 新增 `vocabBank`。
- 从 `jpVocab` 和英语生词卡迁移数据。
- 点击词语时写入 `vocabBank`。
- 根据 `vocabBank` 自动生成复习题。

### 第四步：新增日语语法银行

- 新增 `grammarBank`。
- 从阅读卡的 grammar 字段收藏语法点。
- 语法子模式只读取 `grammarBank`。

### 第五步：重写智能推荐

- 日语：阅读为主，单词/语法为子模式。
- 英语：阅读为主，单词为子模式。
- 通勤模式也要调整：短路段优先单词，长路段优先阅读。

### 第六步：强化 AI

- `/reading-passage` 支持日语和英语。
- `/chat` 支持阅读上下文、收藏词、忘记点、掌握点。
- AI 生成结果必须进入题库，并可评分。

### 第七步：同步和导入导出

- `persistedState()` 纳入 `vocabBank` 和 `grammarBank`。
- `mergeImportedState()` 支持合并这些新结构。
- 保留旧字段一段时间，不做破坏性迁移。

### 第八步：测试

必须测试：

- `node --check app.js`
- `node --check proxy/ai-proxy.mjs`
- `node test.js`
- 打开网页后日语阅读卡是否正常显示
- 点击词语是否能收藏
- 生词是否进入复习题
- AI 对话是否能走 DeepSeek API
- 本地 deepseek-r1:8b 失败时是否有明确提示
- 导出/导入后生词和语法点是否保留

## 风险和注意事项

- 不要一次性删除旧题库。先让新模式跑起来，再逐步降低旧题权重。
- 本地 `deepseek-r1:8b` 速度慢，日语解析质量不稳定，默认仍建议 DeepSeek API。
- 英语不要引入语法子模式，否则这次重构范围会膨胀。
- 阅读卡和生词卡之间要通过 `sourceCardId` 关联，方便回到原文。
- AI 生成短文必须结构化，否则后续很难复习和统计。

## 后续 Codex 协作规则

后续每次你修改完代码，应先更新 `UPDATE_LOG_MANUAL.md`。  
之后你可以直接让我阅读这份更新日志手册，我会基于它给出：

- 哪些改动已经完成
- 哪些地方可能偏离本手册
- 哪些文件还需要调整
- 下一步最小修改建议
- 测试清单是否足够

这份 `MODIFICATION_MANUAL.md` 作为总设计，不需要每次都改；`UPDATE_LOG_MANUAL.md` 作为执行日志，每次都要更新。
